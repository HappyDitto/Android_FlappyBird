<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flappy_bird_basic">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flappy_bird_basic">
        <activity
            android:name=".ProfileLogoutActivity"
            android:exported="false" />
        <activity
            android:name=".result"
            android:exported="true" />
        <activity
            android:name=".OnlineGamingActivity"
            android:exported="false" />
        <activity
            android:name=".LoginRegisterHomeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartGame" />
    </application>

</manifest>